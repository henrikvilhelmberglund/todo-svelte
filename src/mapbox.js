//import mapbox from 'mapbox-gl';
// I don't actually have mapbox, just for reference

// https://docs.mapbox.com/help/glossary/access-token/
//mapbox.accessToken = MAPBOX_ACCESS_TOKEN;

const key = Symbol();

let mapbox = "hi";

export { mapbox, key };