<!-- await stuff (need a slow thingy to actually test)
  {#await promise}
	<p>...waiting</p>
  {:then number}
	<p>The number is {number}</p>
  {:catch error}
	<p style="color: red">{error.message}</p>
  {/await}
  
  or
  
  {#await promise then value}
	<p>the value is {value}</p>
  {/await}
  
-->
